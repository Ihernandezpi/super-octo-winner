{
  "rules": {
    "products":{
       	 ".read": "auth != false",
         ".write": "root.child('admins/'+auth.uid).exists()"
    },
    "productsStructure":{
       	 ".read": "auth != false",
         ".write": "root.child('admins/'+auth.uid).exists()"
    }
  }
}